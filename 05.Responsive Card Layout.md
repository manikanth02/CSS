

```
 <div className={styles.userCardsWrapper}>
          {allUsers?.length > 0 ? allUsers?.map((user) => {
            return <UserCard data={user} onDelete={handleClickOpen} />;
          }) : [1,2,3,4,5,6,7,8,9]?.map(() =>{
            return <UserCardShimmer/>
          }) }
        </div>


```

idea -> We make Container grid, and make coloumns = auto-fit which means it's columns size depnds on avaialble width and we define each column min-length = 400px and when space-increase then 
we increase size by 1fr
```

.userCardsWrapper{
  display: grid !important;
  grid-template-columns: repeat(auto-fit, minmax(400px, 1fr)) !important;
  gap: 10px !important;
}

```

